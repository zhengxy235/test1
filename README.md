<<<<<<< HEAD
张三 - 2025001001 - 第二次人工智能编程作业
=======
郑新译 - 25361051 - 第二次人工智能编程作业
>>>>>>> 953e945789d96763973f403d4af749a889e678d7
1. 任务拆解与 AI 协作策略
我将完整的编程任务按功能模块拆解为 8 个独立子任务，分步向 AI 提出需求，逐步完成开发：
步骤 1：让 AI 实现Student数据类，要求仅包含__init__和__str__魔术方法，无多余逻辑；
步骤 2：让 AI 定义ExamSystem核心类，实现学生数据内置加载功能，完成基础框架搭建；
步骤 3：让 AI 实现学号查找功能，并添加静态方法，满足面向对象编程硬性要求；
步骤 4：让 AI 实现随机点名功能，要求处理ValueError非数字输入异常，限制点名数量；
步骤 5：让 AI 实现考场安排表生成功能，要求文件首行写入指定格式的生成时间；
步骤 6：让 AI 实现准考证生成功能，要求创建文件夹、生成两位数字命名的文件；
步骤 7：让 AI 整合所有功能，编写交互式主菜单，支持循环操作；
步骤 8：让 AI 优化代码格式、添加中文注释，完成最终版本的代码规范调整。
2. 核心 Prompt 迭代记录
初代 Prompt：用 Python 写一个学生考场管理系统，实现查找、点名、生成文件的功能
AI 生成的问题 / 缺陷：1. 未使用面向对象编程，全部用函数实现；2. 未处理任何异常，输入非数字会崩溃；3. 依赖外部文本文件，无内置数据方案；4. 无静态方法，不符合 OOP 规范；5. 生成的文件无指定格式的时间戳。
优化后的 Prompt (追问)：请严格使用 Python 面向对象编程实现学生信息与考场管理系统，仅使用 os/random/datetime 标准库；必须定义 Student 类（含__init__和__str__）和 ExamSystem 类；实现学号查找、随机点名、生成考场表、生成准考证 4 个功能；随机点名必须捕获 ValueError 异常；考场表首行必须为 YYYY-MM-DD HH:MM:SS 格式时间；添加至少 1 个静态方法；将学生数据直接内置到代码中，无需外部文件；代码添加详细中文注释，符合工程规范。
3. Debug 与异常处理记录
报错类型 / 漏洞现象：随机点名功能中，用户输入字母、符号等非数字内容时，程序直接抛出ValueError并崩溃，无法正常运行。
解决过程：我自行查看 Python 的 Traceback 报错信息，定位到count = int(input())这一行；分析原因是int()函数无法转换非数字字符串，触发数值转换异常；我手动添加try-except ValueError代码块捕获异常，并添加友好的中文提示；修改后程序不会崩溃，会提示用户输入有效数字，问题成功解决。
4. 人工代码审查 (Code Review)
def random_call(self):
    # 获取学生总人数，用于限制点名数量范围
    total = len(self.student_list)
    try:
        # 获取用户输入的点名数量，并转换为整数类型
        count = int(input(f"\n请输入点名数量（1~{total}）："))
        # 校验输入的数量是否合法：必须大于等于1，且不超过总人数
        if count < 1 or count > total:
            print(f"❌ 错误：点名数量必须在 1~{total} 之间！")
            return
        # 使用random.sample随机选择不重复的学生，保证点名结果无重复
        random_stus = random.sample(self.student_list, count)
        # 打印随机点名的结果标题
        print(f"\n📌 随机点名结果（共{count}人）：")
        # 遍历选中的学生，按序号格式化输出姓名和学号
        for i, stu in enumerate(random_stus, 1):
            print(f"{i}. 姓名：{stu.name} | 学号：{stu.student_id}")
    # 捕获用户输入非数字内容时触发的ValueError异常
    except ValueError:
        # 输出友好提示，避免程序崩溃
<<<<<<< HEAD
        print("❌ 错误：请输入有效的纯数字！")
=======
        print("❌ 错误：请输入有效的纯数字！")
>>>>>>> 953e945789d96763973f403d4af749a889e678d7
